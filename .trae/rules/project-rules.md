1. ALWAYS use Simplified Chinese to respond
2. MUST run ‘.venv/Scripts/activate’ before running the project
3. USE ‘uv run python xxx.py’ to run the project or files
4. MUST use ‘uv run’ to install project dependencies
4. MUST save project description files in \docs folder
5. MUST save test files in \tests folder
